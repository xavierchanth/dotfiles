interface:
  display_name: "Nix Management"
  short_description: "Manage Nix flakes and rebuild workflows"
  default_prompt: "Use $nix-management to make the requested Nix change, validate affected outputs, and apply it only when requested."
